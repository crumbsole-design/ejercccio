Crear una prueba de integración en tests/test_file_reader.cpp que combine la lectura de archivo (read_real_numbers_from_file) y la normalización, verificando el resultado esperado a partir de un archivo de entrada.

**Criterios de aceptación:**
- Se crea un archivo temporal con valores de prueba.
- Se lee y normaliza el contenido, validando el resultado.
- Se limpia el archivo temporal tras la prueba.