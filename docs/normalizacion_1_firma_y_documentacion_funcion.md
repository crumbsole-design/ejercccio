Definir y documentar la función de normalización de valores reales a rango [0,1] en include/file_reader.h. La función debe aceptar un const std::vector<double>& y devolver un std::vector<double> con los valores normalizados. Documentar el algoritmo y el comportamiento ante valores idénticos.

**Criterios de aceptación:**
- Firma y documentación Doxygen en include/file_reader.h.
- Se especifica el comportamiento para el caso de todos los valores iguales.
- Ejemplo de uso en comentario.