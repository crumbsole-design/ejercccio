Declarar la función `read_real_numbers_from_file` en `include/file_reader.h` con la firma exacta y documentación doxygen.

**Criterios de aceptación:**
- El header declara la función con la firma: `std::vector<double> read_real_numbers_from_file(const std::string& filename);`
- La función está documentada con doxygen.
- No hay implementación, solo declaración.
- No se usan tipos genéricos ni void*.
